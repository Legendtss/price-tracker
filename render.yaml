services:
  - type: web
    name: price-tracker-backend
    env: node
    plan: free
    buildCommand: npm ci && cd backend && npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
